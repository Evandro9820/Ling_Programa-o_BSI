#include <stdio.h>

int main(){
  float n1, n2,n3, menor, maior;

  printf("\n Insira tres valores\n\n");
  scanf("%f%f%f",&n1,&n2,&n3);

  menor = a
  maior = a
    
    if(menor > b)
        menor = b;
    if(menor > c)
        menor = c;

    if(maior < b)
        maior = b;
    if(maior < c)
        maior = c;

    printf("Menor: %f\tMaior: %f\n", menor, maior);
}